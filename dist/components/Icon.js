import * as React from 'react';
export const Icon = ({ type, onClick: handleClick }) => (React.createElement("i", { className: `sg-icon sg-icon-${type}`, onClick: handleClick }));
export default Icon;
//# sourceMappingURL=Icon.js.map